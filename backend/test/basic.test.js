const assert = require('assert');

// A simple test that doesn't require any setup
describe('Basic Test', function() {
  it('should pass a basic assertion', function() {
    assert.strictEqual(1 + 1, 2);
  });
});
